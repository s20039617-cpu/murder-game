# Web Multiplayer MVP

Simple browser-based multiplayer prototype using `express` and `ws`.

Run:

```powershell
cd c:\Users\Alex\Project\Server
npm install
node index.js
```

Open http://localhost:3000 in multiple browser tabs to test joining, start, voting.
